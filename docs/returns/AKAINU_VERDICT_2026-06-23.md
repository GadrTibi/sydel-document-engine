# AKAINU — verdict re-vérification de TOUS les retours (2026-06-23)

> Sweep adversariale (règle 66), 1 auditeur méchant par retour vs verbatim exact.
> TOTAUX : 8 BLOQUANT · 16 MAJEUR · 18 MINEUR · 18 NITPICK. RIEN À REDIRE : O24-13, O24-15.

## LIVE-01 — DEFAUTS
### [NITPICK] Code mort : la fonction `_render_regime_communautaire_form` (ancien toggle GLOBAL « Regime communautaire » avec ses champs conjoint) est definie mais n'est appelee NULLE PART (verifie par grep repo-wide : unique occurrence = sa definition ligne 489). Elle n'est donc PAS rendue dans le formulaire SELAS — le verbatim est respecte (la situation matrimoniale par associe pilote tout, plus de case a cocher) — mais ce residu obsolete contredit l'intention « plus de case a cocher dediee » (cf. commentaire ligne 459-461) et porte en plus des libelles NON ACCENTUES (« Regime communautaire », « Civilite conjoint », « Prenom conjoint », « Nom conjoint » lignes 499/506/511/512) alors que le chemin vif `_render_conjoint_si_communaute` utilise les libelles accentues (« Civilité conjoint », « Prénom conjoint »). Un futur lecteur peut croire le toggle global encore actif.
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:489-521 (definition) ; grep repo-wide : seule occurrence du nom = ligne 489 (aucun appelant)
- **Correction** : Supprimer la fonction morte `_render_regime_communautaire_form` (lignes 489-521) puisque le pilotage du regime se fait desormais exclusivement par la situation matrimoniale par associe (`_render_conjoint_si_communaute`). Si elle doit etre conservee pour une extension future, l'annoter explicitement comme non-cablee et accentuer ses libelles pour parite avec le chemin vif.

## LIVE-02 — DEFAUTS
### [MAJEUR] Le retour LIVE-02 (« Supprimer le champ a cocher "marie sous un regime communautaire" ») est rempli pour la partie RENDUE : le checkbox per-associe (label original « Marie(e) sous un regime communautaire (ajoute renonciation + avertissement au conjoint pour cet associe) ») a bien ete retire de _physique et remplace par le menu « Situation matrimoniale » (selas_multi_slice.py:867-869), et le sous-formulaire conjoint n'apparait que via le menu (selas_multi_slice.py:925-951 ; 46 tests SELAS verts). MAIS un champ a cocher « Regime communautaire » SUBSISTE dans le fichier : la fonction _render_regime_communautaire_form() contient encore st.checkbox("Regime communautaire (ajoute lettre de renonciation + avertissement au conjoint)", key=regime_key). Verifie par grep sur tout le repo (*.py) : cette fonction n'a AUCUN appelant (seule sa propre ligne def matche) — c'est du code mort, jamais rendu a l'operateur. Le verbatim dit « Supprimer » : l'artefact transporte toujours un champ a cocher regime communautaire, reactivable par un simple branchement/copier-colle futur.
- **Preuve** : C:/Users/Gad/Desktop/Sydel/sydel-document-engine-claude/src/sydel_doc_engine/front_app/selas_multi_slice.py:489-521 (checkbox lignes 498-501) — 0 appelant confirme par grep _render_regime_communautaire_form sur tout le repo
- **Correction** : Supprimer integralement la fonction morte _render_regime_communautaire_form() (lignes 489-521), ainsi que le bloc de validation/derivation du toggle global qui ne sert plus que pour des tests directs (validate lignes 1213-1221, branche bool(payload.get('regime_communautaire'))), afin qu'aucun « champ a cocher regime communautaire » ne subsiste dans le code SELAS. Si le chemin toggle-global doit rester pour les tests, le deplacer hors d'une fonction de rendu st.checkbox (constructeur de payload pur) pour qu'aucune case ne soit jamais instanciable cote UI.

### [MINEUR] Le test qui couvre LIVE-02 (test_selas_situation_communaute_genere_docs_conjoint) prouve que le MENU genere bien DOC-005/006, mais n'assert JAMAIS l'absence d'un champ a cocher « regime communautaire » dans le formulaire SELAS rendu. Une regression qui re-brancherait le checkbox (ex. re-appel de _render_regime_communautaire_form, encore presente) ne serait pas detectee. La suppression demandee par le verbatim n'est pas verrouillee par un test negatif.
- **Preuve** : C:/Users/Gad/Desktop/Sydel/sydel-document-engine-claude/tests/unit/test_multi_type_front.py:2409-2448 (aucune assertion d'absence de checkbox)
- **Correction** : Ajouter une assertion negative au test SELAS : apres rendu du formulaire SELAS pluripersonnelle, verifier qu'aucun widget checkbox du formulaire ne porte un label contenant « regime communautaire » / « marie sous » (ex. assert not any('communautaire' in str(c.label).lower() for c in app.checkbox)), afin de verrouiller la suppression demandee par LIVE-02.

### [NITPICK] Provenance incoherente de la decision de suppression du checkbox dans une source citee comme reference. Le docstring de regime_communautaire_from_status attribue le retrait de la case a « retours client 2026-06-11 », alors que la suppression SELAS effective est portee par R10/R11 date 2026-06-23 (commentaires en-code selas_multi_slice.py:864-866 + commit 9165664). Meme decision, deux dates/sources divergentes — bruit pour l'audit de tracabilite.
- **Preuve** : C:/Users/Gad/Desktop/Sydel/sydel-document-engine-claude/src/sydel_doc_engine/front_app/field_derivations.py:219-220 (« plus de case a cocher dediee, retours client 2026-06-11 »)
- **Correction** : Aligner la date/source du docstring sur la decision SELAS effective (R10/R11, Rafael 2026-06-23) ou citer les deux jalons sans ambiguite, pour une tracabilite coherente du retrait du champ a cocher.

## LIVE-03 — DEFAUTS
### [MAJEUR] Un nom de mois NON accentue atteint la SORTIE en violation directe du verbatim LIVE-03 (« mois accentues partout en SORTIE »). La fixture scm_cession_fixture porte agrement.date_limite = "15 aout 2026" (aout sans accent circonflexe). Cette fixture est chargee telle quelle comme payload de base du sous-formulaire SCM-cession de la SELAS pluripersonnelle (front_app/shell.py:59 import, shell.py:2632 base = scm_cession_fixture(), payload = base.model_dump). Le bloc agrement n'est JAMAIS re-expose ni recalcule dans _render_scm_cession_subform : le sous-formulaire retourne ScmCessionContext.model_validate(payload) a shell.py:2771 en heritant agrement.date_limite intact. La branche SELAS du generateur emet ensuite cette valeur VERBATIM dans le PV : generators/lot_05/pv_age_cession_scm.py:239 -> "...soit jusqu'au {required_text(scm_cession.agrement.date_limite,...)}." => le document de sortie contient « soit jusqu'au 15 aout 2026. ». Confirme au runtime : scm_cession_fixture().agrement.date_limite == '15 aout 2026'.
- **Preuve** : src/sydel_doc_engine/scenarios/selarl.py:446 : "date_limite": "15 aout 2026" ; emission verbatim en sortie src/sydel_doc_engine/generators/lot_05/pv_age_cession_scm.py:239 ; chemin SELAS pv_age_cession_scm.py:236 (if ctx.structure == "SELAS")
- **Correction** : Remplacer scenarios/selarl.py:446 "15 aout 2026" par "15 août 2026" (aout -> août). Aligner la fixture sur la regle mois-accentues-en-SORTIE, identique a la valeur deja accentuee dans le test (test_lot_05_scm_cession.py:155).

### [MAJEUR] Test faible / inconsistance qui masque le defaut. Le test SCM-cession SELAS n'exerce PAS la fixture de production scm_cession_fixture() : il reconstruit son propre ScmCessionAgrement avec date_limite="15 août 2026" (accentue) puis assert "15 août 2026" in pv_text. La suite reste donc verte alors que la fixture reellement chargee par le shell (et donc par le pack reproductible « meme scenario -> memes documents », cf. docstring scenarios/selarl.py:1-6) expedie "15 aout 2026" non accentue en sortie. Aucun test ne couvre la valeur par defaut realment shippee.
- **Preuve** : tests/unit/test_lot_05_scm_cession.py:155 (date_limite="15 août 2026" if is_selas else None) et :265 (assert "15 août 2026" in pv_text) — input hardcode accentue, fixture jamais sollicitee
- **Correction** : Ajouter un test qui genere le PV SELAS a partir de scm_cession_fixture() (ou de la sortie de _render_scm_cession_subform) et assert qu'AUCUN mois non accentue (aout/fevrier/decembre) n'apparait dans le texte produit ; ce test doit echouer tant que selarl.py:446 n'est pas corrige.

### [MINEUR] Incoherence interne : la meme valeur logique (date limite d'agrement) est ecrite accentuee dans le test ("15 août 2026", test_lot_05_scm_cession.py:155) mais non accentuee dans la fixture de production consommee par le shell ("15 aout 2026", scenarios/selarl.py:446). Deux representations divergentes de la meme donnee canon.
- **Preuve** : scenarios/selarl.py:446 "15 aout 2026" vs tests/unit/test_lot_05_scm_cession.py:155 "15 août 2026"
- **Correction** : Unifier sur la forme accentuee "15 août 2026" cote fixture (corrige par le defaut MAJEUR ci-dessus) ; idealement faire deriver test et fixture de la meme source pour eviter la divergence.

### [NITPICK] Docstring desynchronisee : le docstring annonce fin='31 decembre' (sans accent) alors que la valeur reellement seedee a la ligne suivante est "31 décembre" (accentuee, correcte). Cosmetique, n'affecte pas la SORTIE, mais induit en erreur le lecteur sur la convention mois-accentues.
- **Preuve** : src/sydel_doc_engine/front_app/front_widgets.py:92 (docstring "...fin='31 decembre'...") vs :94 (seed_if_empty(..., "31 décembre"))
- **Correction** : Corriger le docstring front_widgets.py:92 en '31 décembre' pour refleter la valeur seedee et la convention de sortie accentuee.

## LIVE-04 — DEFAUTS
### [MINEUR] La generation conjointe acte+compromis en SELAS repose entierement sur la branche orchestrateur `_cession_cabinet_enabled` (service.py:365-366 : `if (ctx.structure or '').strip().upper().startswith('SELAS'): return True`). Cette branche - qui EST le coeur du « les deux sont generes ensemble » du verbatim - n'a AUCUN test de regression au niveau orchestrateur. Verifie moi-meme au runtime : structure=SELAS medical/etape=acte -> {DOC-009:True, DOC-010:True} ; dentaire -> {DOC-011:True, DOC-012:True} ; SELARL -> seul l'acte (correct). Les tests existants ne couvrent QUE le plan (`test_selas_cession_genere_acte_et_compromis_ensemble` ligne 2059 teste `_cession_codes`, pas l'orchestrateur) et l'absence du menu UI (`test_selas_cession_vendeur_selectionnable` ligne 2096). Le seul test orchestrateur de selection cession (`test_orchestrator_selects_only_requested_cession_cabinet_document`, test_cession_cabinets.py:602) utilise structure=SELARL et asserte un SEUL document. Un revert de la branche SELAS laisserait le plan annoncer 2 codes mais l'orchestrateur n'en generer qu'1, en silence, sans casser aucun test.
- **Preuve** : src/sydel_doc_engine/orchestrator/service.py:365-366 (branche SELAS non testee) ; tests/unit/test_cession_cabinets.py:602-615 (seul test de selection = SELARL, 1 doc) ; tests/unit/test_multi_type_front.py:2059-2072 (teste _cession_codes, pas l'orchestrateur)
- **Correction** : Ajouter un test orchestrateur qui construit un DocumentGenerationContext structure='SELAS' + cession (medical puis dentaire, etape='acte') et asserte que select_documents_for_context retourne A LA FOIS l'acte ET le compromis (DOC-009+DOC-010 / DOC-011+DOC-012), pour figer la branche `startswith('SELAS')` contre toute regression silencieuse.

### [NITPICK] Le titre de l'expander de cession reste « Type & etape » (shell.py:1932) alors qu'en SELAS l'etape (menu retire par ce retour) ET le type (devenu une simple caption derivee, shell.py:1942) ont tous deux disparu : l'expander SELAS ne contient ni selecteur « Type » ni selecteur « Etape », mais affiche encore un libelle promettant les deux. Libelle desormais trompeur dans le contexte SELAS vise par le retour LIVE-04.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:1932 `with st.expander("Type & etape", expanded=True):` combine aux branches SELAS lignes 1939-1942 (type en caption) et 1954-1956 (etape forcee, aucun widget)
- **Correction** : En SELAS, intituler l'expander de facon neutre (ex. « Cession - parametres » ou « Type de cabinet ») puisque ni le choix de type ni le choix d'etape n'y sont plus offerts ; conserver « Type & etape » uniquement pour la branche non-SELAS.

### [NITPICK] En SELAS, la colonne `col_b` du `st.columns(2)` (shell.py:1933) reste vide : `col_a` porte la caption du type derive (1942) mais aucun widget n'est place dans `col_b` puisque l'etape est forcee sans selectbox (1954-1956). Resultat : une demi-largeur vide a cote de la caption de type. Asymetrie cosmetique introduite par le retrait du menu Etape.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:1933 `col_a, col_b = st.columns(2)` ; en branche SELAS `col_b` n'est jamais utilise (1954-1956 n'appellent pas col_b.selectbox)
- **Correction** : En SELAS, ne pas creer deux colonnes (ou n'utiliser qu'une seule colonne pleine largeur pour la caption du type) afin d'eviter la colonne fantome ; conserver `st.columns(2)` seulement pour la branche non-SELAS qui remplit col_a (Type) et col_b (Etape).

## O24-01 — DEFAUTS
### [BLOQUANT] Retour O24-01 NON appliqué au SELAS pluripersonnelle (profil médecin) — le formulaire explicitement visé par la mission. Le modèle source Statuts_SELAS_multi_modele.docx contient encore dans son ANNEXE les 2 items à supprimer (paras 547 « Signature d'une lettre de mission avec le cabinet Sydel pour la création de la Société » et 548 « Paiement de l'acompte des honoraires du cabinet Sydel pour la création de la Société »). Le générateur statuts_selas_multi.py rend chaque paragraphe du modèle 'tel quel' hors fenêtres dynamiques (boucle lignes 115-149) : l'annexe n'est dans AUCUNE fenêtre de réinjection, donc les 2 items passent dans le DOCX final. Reproduit sur DOCX régénéré CETTE SESSION (mtime 2026-06-23 14:14) : artifacts/_audit_tmp/pt2/test_selas_multi_generates_cle0/statuts_selas_multi.docx paras [258]/[259] contiennent bien les 2 items.
- **Preuve** : project/source_documents/lot_04/Statuts_SELAS_multi_modele.docx paras 547-548 (source) ; src/sydel_doc_engine/generators/lot_04/statuts_selas_multi.py:115-149 (rendu tel-quel sans filtre) ; DOCX régénéré session artifacts/_audit_tmp/pt2/test_selas_multi_generates_cle0/statuts_selas_multi.docx paras [258]/[259]
- **Correction** : Retirer les 2 paragraphes (lettre de mission + acompte honoraires) de l'ANNEXE du modèle source Statuts_SELAS_multi_modele.docx (ne conserver que « Ouverture d'un compte bancaire »), OU filtrer ces 2 lignes dans la boucle de rendu de statuts_selas_multi.py. Aligner sur le modèle dentiste Statuts_SELAS_dentiste_pluri_modele.docx qui, lui, est déjà conforme (annexe = Ouverture d'un compte bancaire uniquement).

### [BLOQUANT] Retour O24-01 NON appliqué au type SCI IRIS (type LIVE exposé dans le front, civil_statuts_slice.py:71 « SCI IRIS » -> sci_iris / DOC-021). Le modèle source « Modèle statuts SCI IRIS.docx » contient encore les 2 items interdits dans l'annexe (paras 662-663), et generate_statuts_civil_docx (statuts_civils_common.py:114-149) les rend tel quel. Reproduit sur DOCX régénéré CETTE SESSION via le code actuel (pytest -k iris) : artifacts/_audit_tmp/iris_fresh/test_statuts_sci_iris_generate0/statuts_sci_iris.docx -> 'lettre de mission': True / 'acompte des honoraires': True.
- **Preuve** : project/source_documents/lot_04/Modèle statuts SCI IRIS.docx paras 662-663 ; src/sydel_doc_engine/generators/lot_04/statuts_sci_iris.py:15-16 + statuts_civils_common.py:114-149 ; DOCX régénéré session artifacts/_audit_tmp/iris_fresh/test_statuts_sci_iris_generate0/statuts_sci_iris.docx (vérifié: les 2 chaînes présentes)
- **Correction** : Retirer les 2 paragraphes (lettre de mission + acompte) de l'annexe du modèle source « Modèle statuts SCI IRIS.docx » (les deux copies lot_04/ et sci/), OU les filtrer dans generate_statuts_civil_docx.

### [BLOQUANT] Retour O24-01 NON appliqué au type SCI (civile standard, type LIVE — civil_statuts_slice.py:70 « SCI » -> sci / DOC-020). Le modèle source « Modèle statuts SCI.docx » contient encore les 2 items interdits dans l'annexe (paras 672-673). Le DOCX statuts_sci.docx régénéré cette session figure dans la liste des fichiers AFFECTÉS (contient encore lettre de mission + acompte).
- **Preuve** : project/source_documents/lot_04/Modèle statuts SCI.docx paras 672-673 ; rendu via statuts_civils_common.py:114-149 ; classé AFFECTED dans le scan session des DOCX (statuts_sci.docx)
- **Correction** : Retirer les 2 paragraphes de l'annexe du modèle source « Modèle statuts SCI.docx » (copies lot_04/ et sci/), ou les filtrer au rendu.

### [BLOQUANT] Retour O24-01 NON appliqué au type SCS (type LIVE — civil_statuts_slice.py:72 « SCS » -> scs / DOC-019). Le modèle source Statuts_SCS_modele.docx contient encore les 2 items interdits dans l'annexe (paras 306-307), formulés avec le placeholder [denomination_cabinet_mandataire] mais sémantiquement identiques au verbatim à supprimer. Le DOCX statuts_scs.docx régénéré cette session figure dans la liste des fichiers AFFECTÉS.
- **Preuve** : project/source_documents/lot_04/Statuts_SCS_modele.docx paras 306-307 ; rendu via statuts_civils_common.py:114-149 ; classé AFFECTED dans le scan session des DOCX (statuts_scs.docx)
- **Correction** : Retirer les 2 paragraphes (Signature d'une lettre de mission… / Paiement de l'acompte des honoraires…) de l'annexe du modèle source Statuts_SCS_modele.docx (copies lot_04/ et scs/), ou les filtrer au rendu.

### [MAJEUR] Couverture de test insuffisante au regard du verbatim « TOUS les cas ». La seule assertion de non-régression (assert 'lettre de mission' not in text / assert 'acompte des honoraires' not in text) n'existe que pour le SPFPL cession. Aucun test ne garde l'absence de ces items pour SELAS multi, SCI, SCI IRIS, SCS — ce qui explique que la suite reste à 100% verte alors que 4 types violent le verbatim. Le test passe (3 verts SPFPL vérifiés cette session) mais ne couvre que ~1/5 du périmètre exigé.
- **Preuve** : tests/unit/test_lot_04_statuts_spfpl.py:206-207 (seules assertions) ; aucune assertion équivalente dans test_lot_04_statuts_sel_exercice.py, test_lot_04_statuts_selas_multi.py, test_lot_04_statuts_civils.py
- **Correction** : Ajouter, pour CHAQUE type de statuts généré (SAS/SPFPL apport+cession, SELARL médecin+dentiste, SELAS médecin, SELAS multi médecin+dentiste, SCI, SCI IRIS, SCS, SCM), une assertion 'lettre de mission' not in text et 'acompte des honoraires' not in text sur le DOCX rendu, conformément au verbatim « tous les statuts de tous les cas ».

### [MAJEUR] Fausse clôture / affirmation non vérifiée dans le suivi des retours. Le carnet déclare O24-01 « TRAITÉ » sur « tous types » et la note de triage affirme explicitement « O24-01/02/05 déjà propagés ». C'est contredit par l'état réel du code : 4 types (SELAS multi médecin, SCI, SCI IRIS, SCS) rendent toujours les 2 items. La déclaration de traitement induit en erreur sur la complétude.
- **Preuve** : docs/returns/CARNET.md:14 (« tous types | TRAITÉ ») et docs/returns/CARNET.md:46 (« O24-01/02/05 déjà propagés »)
- **Correction** : Repasser O24-01 en NON TRAITÉ / partiel tant que les 4 types ne sont pas corrigés ; ne marquer TRAITÉ qu'après vérification du rendu réel de TOUS les types, pas seulement du SPFPL.

### [MINEUR] Écart de fidélité du verbatim sur le SCS : l'item interdit y est paramétré « cabinet [denomination_cabinet_mandataire] » (placeholder) et non « cabinet Sydel » en dur. Le verbatim O24-01 vise nommément « cabinet Sydel » ; la suppression demandée s'applique néanmoins (même item d'annexe), mais le libellé source diverge des autres modèles — à signaler pour ne pas le rater lors de la correction par simple recherche littérale « Sydel ».
- **Preuve** : project/source_documents/lot_04/Statuts_SCS_modele.docx paras 306-307 (« avec le cabinet [denomination_cabinet_mandataire] »)
- **Correction** : Lors de la suppression, cibler l'item par sa structure (« Signature d'une lettre de mission … » / « Paiement de l'acompte des honoraires … ») et non par la chaîne littérale « Sydel », sinon l'occurrence SCS serait manquée.

## O24-02 — DEFAUTS
### [MINEUR] SELAS multi — collision d'homonymes non gérée pour le nom du document DNC. _associe_filename_slug (selas_multi_slice.py:2027-2038) construit le slug à partir du NOM SEUL (`associe.nom or associe.prenom`). Si deux dirigeants partagent le même nom de famille (cas courant en cabinet familial : père/fils, fratrie, tous deux associés et cochés « Dirigeant »), _rename_with_slug (selas_multi_slice.py:2041-2057) suffixe `_2` → produit `declaration_non_condamnation_Dupont.docx` et `declaration_non_condamnation_Dupont_2.docx`. Le `_2` ne permet PAS d'identifier QUEL Dupont, ce qui contredit frontalement le but explicite du verbatim : « intégrer le nom du dirigeant de manière à POUVOIR RETROUVER LES BONS DOCUMENTS ». Aucun test ne couvre ce cas (grep collision/homonyme/_2.docx dans tests/ = 0 résultat → edge-case non couvert).
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:2033 (`base = (associe.nom or associe.prenom or associe.prenoms or "associe")`) + :2047-2055 (suffixe `_index` en cas de collision)
- **Correction** : Inclure le prénom dans le slug du dirigeant en SELAS multi (ex. `Nom_Prenom`) ou au minimum désambiguïser par prénom quand deux dirigeants partagent le nom, au lieu d'un suffixe numérique opaque. Ajouter un test à deux dirigeants homonymes vérifiant que chaque DNC reste identifiable par son nom.

### [MINEUR] Test de complétude faible qui déguise une éventuelle absence de renommage (rule 65 §8). _assert_bundle_clean (tests/unit/test_multi_type_front.py:183-193) NORMALISE tout fichier dont le nom commence par `declaration_non_condamnation` vers le nom générique `declaration_non_condamnation.docx` avant de comparer au bundle attendu. Conséquence : un bundle où le renommage O24-02 aurait SILENCIEUSEMENT échoué (fichier resté `declaration_non_condamnation.docx`, sans nom de dirigeant) passe quand même ces tests, car le `startswith` matche aussi bien le fichier renommé que le non-renommé. L'exigence verbatim (nom du dirigeant dans le nom de fichier) n'est donc PAS gardée par les tests de complétude des bundles (SCI/SCS/SCM/SAS/SPFPL/SELAS) ; elle ne repose QUE sur 2 tests dédiés (SELARL clean ligne 675-676 + test_selas_dnc_une_par_dirigeant ligne 2367). Les bundles SAS/SPFPL/civil/SELAS-uni n'ont donc aucune assertion vérifiant réellement le nom du dirigeant dans leur DNC.
- **Preuve** : tests/unit/test_multi_type_front.py:187-192 (`"declaration_non_condamnation.docx" if p.name.startswith("declaration_non_condamnation") else p.name`)
- **Correction** : Dans _assert_bundle_clean (ou un test dédié par type), asserter que le bundle contient bien `declaration_non_condamnation_<Nom>.docx` ET PAS le nom générique `declaration_non_condamnation.docx`, au moins pour SAS/SPFPL/civil/SELAS-uni qui n'ont aujourd'hui aucune assertion de nommage.

### [NITPICK] Fidélité partielle de la docstring du helper mono au verbatim. rename_dnc_with_signataire (src/sydel_doc_engine/app/ui_runtime.py:160-165) ne cite que la moitié « naming » du verbatim (« le nom du document doit intégrer le nom du dirigeant ») et omet la moitié « elle doit être générée d'office pour chaque dirigeant uniquement ». Le comportement reste correct (le cas multi est traité ailleurs), mais la citation verbatim incomplète peut induire en erreur un relecteur sur la portée de la règle O24-02.
- **Preuve** : src/sydel_doc_engine/app/ui_runtime.py:160-165
- **Correction** : Compléter la docstring en citant le verbatim entier et en renvoyant explicitement au traitement multi-dirigeants (_generate_dnc_par_dirigeant) pour la clause « une par dirigeant ».

## O24-03 — DEFAUTS
### [BLOQUANT] PERIMETRE INCOMPLET vs verbatim. Le verbatim impose « TOUTES les adresses sur une ligne, pas de champ separe ». Or l'adresse de l'ORDRE PROFESSIONNEL du formulaire SELAS est encore eclatee en TROIS champs separes : « Adresse ordre » (ordre_adresse_ligne_1), « CP ordre » (ordre_cp), « Ville ordre » (ordre_ville). Ces 3 champs sont rendus dans _render_common_docs_form, appele depuis render_selas_form (ligne 363), et tous les trois sont requis (validation lignes 1257-1259). Le commit O24-03 (9780c4f) n'a converti QUE l'adresse perso + le siege ; l'adresse de l'ordre a ete oubliee. L'implementation n'est donc PAS pleine ni fidele au verbatim (« toutes les adresses », « etc. »).
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:434-436 (ordre_ligne='Adresse ordre', ordre_cp='CP ordre', ordre_ville='Ville ordre') ; validation 1257-1259 ; mapping OrdreAddress(ligne_1, cp, ville) 1684-1688
- **Correction** : Remplacer les 3 champs ordre par UN seul champ texte (ex. « Adresse de l'ordre (N° et voie, CP Ville) »), parse interne via _parse_address_full pour alimenter ligne_1/cp/ville d'OrdreAddress, exactement comme cela a ete fait pour perso + siege. Adapter la validation (un seul blocker « Adresse de l'ordre requise »).

### [MAJEUR] INCOHERENCE / DOUBLE-SAISIE introduite : le formulaire SELAS rend desormais DEUX champs d'adresse de siege simultanement. Ligne 289 « Siege (adresse affichee) » (key selas_siege) -> payload['siege'], champ REQUIS (1124) et utilise comme adresse affichee du siege (1441). Ligne 338 « Adresse du siège (N° et voie, CP Ville) » (key selas_siege_adresse) -> parse en siege_num/voie/cp/ville. L'operateur doit donc saisir l'adresse du siege DEUX FOIS, et les deux peuvent diverger. Cela contredit le principe anti-double-saisie revendique par le module lui-meme (RAF-003) et brouille le sens du verbatim (une adresse = un champ).
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:289 (siege affichee, requis 1124) vs 338 (siege_adresse parse) ; lecture payload.get('siege') en 1441
- **Correction** : Unifier en UN seul champ d'adresse de siege : soit deriver l'affichage (payload['siege']) de _parse_address_full(siege_ligne).adresse_affichee et supprimer le champ 289, soit supprimer le champ 338 et reutiliser le 289 pour le parse. Retirer le doublon de la validation.

### [MAJEUR] CAS-LIMITE NON COUVERT dans _parse_address_full : des adresses francaises legitimes saisies sur UNE ligne sont silencieusement rejetees (retour None) -> traitees comme « adresse requise » et generation bloquee. Verifie par execution : « 12, rue de la Paix, 75001 Paris » (virgule apres le numero, format tres courant) -> None ; « 12 rue de la Paix 75001 Paris » (sans virgule) -> None. partition(',') coupe sur la 1re virgule, donc une virgule apres le numero casse tout le parsing. Le verbatim impose la saisie sur une ligne : le parseur doit accepter les formes une-ligne usuelles, pas seulement « num voie, cp ville » exact.
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:984-1008 (_parse_address_full, raw.partition(',')) ; reproduction : input '12, rue de la Paix, 75001 Paris' -> None
- **Correction** : Rendre le parse tolerant : detacher le numero de tete AVANT de partitionner, et/ou splitter sur la DERNIERE virgule (ou rpartition) pour isoler « CP Ville », tolerer l'absence de virgule en detectant le motif CP(4-5 chiffres)+Ville en fin de chaine. Ajouter des cas de test pour « 12, rue ... » et l'absence de virgule.

### [MINEUR] TEST FAIBLE : test_selas_adresses_sur_une_ligne n'assert QUE les 2 champs touches par le commit (selas_siege_adresse et selas_associe_0_adresse) et l'absence des anciennes cles structurees. Il n'assert RIEN sur l'adresse de l'ordre (toujours eclatee en 3) ni sur le caractere unique de l'adresse du siege. Le test ne peut donc pas detecter le perimetre incomplet (BLOQUANT ci-dessus) ni le doublon de siege : il valide un sous-ensemble en se presentant comme la conformite « toutes les adresses ».
- **Preuve** : tests/unit/test_multi_type_front.py : test_selas_adresses_sur_une_ligne (introduit par 9780c4f) - asserts limites a selas_siege_adresse / selas_associe_0_adresse / absence de selas_siege_voie/num
- **Correction** : Etendre le test pour assurer qu'AUCUN champ d'adresse separe ne subsiste dans le formulaire SELAS : asserter l'absence des cles selas_ordre_cp / selas_ordre_ville (apres correction), la presence d'un champ ordre unique, et qu'il n'existe pas deux champs de siege simultanes.

### [NITPICK] Le docstring de _parse_address_full cite le verbatim de facon TRONQUEE : « Toutes les adresses sur une ligne, pas de champ separe pour la rue, la voie. » alors que le verbatim client se termine par « ..., la voie, etc.. ». Le « etc. » (qui justement etend l'exigence a TOUTES les sous-parties d'adresse, ordre compris) est omis -> paraphrase du verbatim dans le code, a proscrire (la spec = le verbatim mot pour mot).
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:976-977 (docstring)
- **Correction** : Citer le verbatim integral (« ..., la voie, etc. ») ou ne pas le paraphraser dans le commentaire.

## O24-05 — DEFAUTS
### [MAJEUR] Perimetre incomplet vs verbatim. Le CARNET (docs/returns/CARNET.md:18) declare O24-05 en perimetre « tous types » et statut TRAITE. Or le type SELARL — PREMIERE entree du registre (type_registry.py:44-51, selarl_v1) et chemin de creation reel — n'affiche PAS la valeur nominale « dans le champ concerne » : dans _render_societe (shell.py:1496-1500), la valeur calculee est rendue via st.caption (« Valeur nominale calculee : X EUR »), c'est-a-dire un texte d'aide gris, pas un champ. Le verbatim exige : « calculee automatiquement ET affichee dans le champ concerne ». Le commit O24-05 (18feb29) ne touche pas shell.py (modifie seulement civil/sas/spfpl/selas_uni). La SELARL reste donc non conforme.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:1496-1500 (st.caption au lieu d'un champ) ; chemin appele depuis shell.py:1121 _render_societe ; perimetre « tous types » : docs/returns/CARNET.md:18 ; commit 18feb29 ne contient pas shell.py
- **Correction** : Remplacer le st.caption SELARL par un st.text_input("Valeur nominale d'une part (calculee)", value=valeur_nominale_part, disabled=True) — meme patron que les 5 autres types — pour afficher la valeur dans un champ. Sinon, faire valider explicitement par le client que le caption suffit pour la SELARL et restreindre le perimetre du retour.

### [MINEUR] Incoherence interne / ecart au verbatim « calculee automatiquement » dans le sous-formulaire cession (SCM cedee). La valeur nominale d'une part de la SCM cedee est saisie en TEXTE LIBRE editable (_cession_text), alors que le capital social (shell.py:2654) et le nombre total de parts (shell.py:2658) de cette meme SCM sont saisis juste au-dessus : la valeur nominale pourrait donc etre auto-calculee comme partout ailleurs, mais ne l'est pas. Avec perimetre « tous types » au CARNET, c'est un point non couvert/incoherent.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:2668-2672 (valeur_nominale_part en _cession_text libre) vs capital shell.py:2654 et nb_parts shell.py:2658 saisis ; commentaire « nominal pilotable » shell.py:2650-2652
- **Correction** : Soit auto-calculer la valeur nominale de la SCM cedee (capital / nb_parts) et l'afficher en champ disabled comme ailleurs ; soit faire confirmer au client que la SCM cedee (entite externe) est volontairement exclue de O24-05 et le tracer dans le CARNET (le perimetre « tous types » laisse aujourd'hui croire l'inverse).

### [MINEUR] Test faible : aucun test n'assure que la valeur nominale calculee est REELLEMENT AFFICHEE (« affichee dans le champ concerne »), pour aucun type. Les tests front (ex. test_multi_type_front.py:1793+) se contentent de saisir des inputs et de verifier que la generation reussit / le bouton n'est pas bloque ; aucune assertion ne lit le champ « Valeur nominale ... (calculee) » ni ne verifie sa valeur. La partie « affichage » du verbatim O24-05 n'est donc pas couverte ; une regression (champ vide, valeur figee, key= reintroduit) passerait inapercue.
- **Preuve** : tests/unit/test_multi_type_front.py:1804-1839 (set inputs + generation seulement, pas d'assertion sur le champ valeur nominale) ; recherche « calculee/disabled/champ concerne » sans resultat dans test_clean_front_app.py et test_multi_type_front.py
- **Correction** : Ajouter, pour chaque type concerne, une assertion qui (1) saisit capital + nb parts/actions divisibles, (2) relit le text_input « Valeur nominale ... (calculee) » et verifie value == capital/nb, (3) verifie disabled is True et l'absence de key fige (rerun -> la valeur se met a jour).

### [NITPICK] Faute d'accent dans un libelle utilisateur. Le label du champ est « Valeur nominale d'une action/part (calculee) » — « calculee » sans accent, alors que les autres libellés user-facing des memes fichiers sont accentues (ex. selas_multi_slice.py:338 « Adresse du siège », :457 « Conseiller (prénom) »). Devrait etre « (calculée) ». 5 occurrences identiques.
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:318, sas_slice.py:138, spfpl_slice.py:214, selas_uni_medecin_slice.py:158, civil_statuts_slice.py:238 (« (calculee) ») ; comparatif accentue : selas_multi_slice.py:338, :457
- **Correction** : Remplacer « (calculee) » par « (calculée) » dans les 5 labels de champ valeur nominale (les commentaires de code peuvent rester non accentues, mais le libelle affiche a l'utilisateur doit l'etre).

## O24-06 — DEFAUTS
### [NITPICK] Commentaire perime : il decrit un mecanisme de prefill specifique a la cle de type ("SELAS dentiste pluripersonnelle -> prefill SELAS en dentiste") qui n'existe PLUS. _TYPED_TEST_DATA_PREFILL_BY_KEY est un dict VIDE (shell.py:863), donc .get(selected.key) renvoie toujours None et le code retombe sur le prefill par structure. Le commentaire suggere a tort qu'un cas dentiste distinct route differemment, ce qui contredit le verbatim 'un seul cas dans le moteur'.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:98-99 ("Une cle de type peut surcharger le prefill par defaut de sa structure (ex. SELAS dentiste pluripersonnelle -> prefill SELAS en dentiste).") vs shell.py:863 (_TYPED_TEST_DATA_PREFILL_BY_KEY = {})
- **Correction** : Reformuler le commentaire shell.py:98-99 pour indiquer que la surcharge par cle est desormais inutilisee (dict vide) et que la SELAS pluripersonnelle unique choisit la profession DANS le formulaire ; ou supprimer la reference au cas 'dentiste pluripersonnelle' supprime.

### [NITPICK] Docstring perimee : indique que la profession dentiste est utilisee 'pour le cas nomme SELAS dentiste pluripersonnelle'. Ce cas nomme n'existe plus (fusionne dans 'SELAS pluripersonnelle creation V1' unique, type_registry.py:123-130). La profession est desormais choisie au menu dans le formulaire unique, pas attachee a un cas nomme separe.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:788-790 ("« chirurgien-dentiste » pour le cas nomme SELAS dentiste pluripersonnelle")
- **Correction** : Mettre a jour la docstring de _prefill_selas_test_data : la profession parametrable sert les donnees de test du cas unique 'SELAS pluripersonnelle' ; retirer la mention d'un 'cas nomme SELAS dentiste pluripersonnelle' distinct.

### [NITPICK] Commentaire perime dans le routing : laisse entendre qu'on passe la cle du type pour qu'une SELAS 'dentiste pluripersonnelle' pre-regle la profession sur chirurgien-dentiste. Or il n'y a plus de pre-reglage par cle (selas_multi_slice._PROFESSION_DEFAULTS_BY_TYPE est vide, ligne 106 ; _apply_type_profession_default ne fait rien sans entree). La cle est encore passee mais n'a plus d'effet de pre-reglage profession.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:3081-3082 ("On passe la cle du type pour que la SELAS « dentiste pluripersonnelle » pre-regle la profession sur chirurgien-dentiste (corpus dentiste).") vs selas_multi_slice.py:106 (_PROFESSION_DEFAULTS_BY_TYPE = {})
- **Correction** : Corriger le commentaire shell.py:3081-3082 : la cle est transmise pour extension future mais ne pre-regle plus aucune profession (profession choisie au menu du formulaire unique).

## O24-07 — DEFAUTS
### [MAJEUR] Incohérence interne : un associé à qui l'opérateur attribue explicitement le rôle « Directeur Général » (ou « Directeur Général Associé ») est silencieusement requalifié « Président » par le fallback de _derive_president_index, dès qu'aucun associé n'a explicitement le rôle « Président ». Scénario reproductible : associé 0 = « Directeur Général », associé 1 = « Directeur Général Associé », aucun « Président ». _validate_roles_dirigeants ne bloque pas (count Président=0, count DG=1). _derive_president_index étape 1 échoue, étape 2 renvoie le 1er dirigeant physique = associé 0. _collect_dirigeants_nomines_indices(…, president_index=0) ajoute alors (0, 'Président') en 1re décision PV, écrasant le rôle « Directeur Général » choisi. L'acte/PV nomme Président un associé que l'opérateur a désigné DG, sans aucune alerte — ce qui contredit le verbatim « soit président, soit directeur général » (fonctions distinctes).
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:675-685 (_derive_president_index fallback) + 701-711 (_collect_dirigeants_nomines_indices force role='Président' ligne 703) + 715-737 (_validate_roles_dirigeants n'examine que >1)
- **Correction** : Soit bloquer/avertir quand un dirigeant explicitement « Directeur Général » est requalifié Président par fallback, soit respecter le rôle choisi (ne pas écraser un rôle DG/DGA explicite par 'Président'). Aligner le rôle généré au PV sur le rôle réellement choisi dans le menu.

### [MAJEUR] Périmètre incomplet vs verbatim : le verbatim pose « soit président (UN seul) » comme fonction de tête, mais aucune validation n'exige la présence d'AU MOINS un Président. On peut générer un dossier sans aucun associé « Président » (que des DG/DGA) ; le système ne bloque pas et fabrique un président par fallback (cf. défaut précédent). La cardinalité « exactement un Président » du verbatim n'est donc garantie que par le haut (max 1) et non par le bas (min 1).
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:715-737 (_validate_roles_dirigeants ne vérifie que roles.count('Président')>1 et count('Directeur Général')>1 ; aucune borne basse) + _validate lignes 1120-1187 (aucune exigence d'un Président explicite)
- **Correction** : Ajouter un blocker quand aucun associé physique dirigeant n'a le rôle « Président » (un Président est obligatoire). Couvrir par un test « 0 Président → blocage ».

### [MINEUR] Test faible : aucun test ne couvre les deux assertions les plus directes du verbatim O24-07. (a) « président UN SEUL » : aucun test ne vérifie que 2 associés « Président » déclenchent le blocage « Un seul Président est admis ». (b) « directeur général associé PLUSIEURS » : test_selas_role_directeur_general_associe ne met qu'UN seul DGA — la non-limitation (plusieurs DGA admis simultanément) n'est jamais exercée. La branche roles.count('Président')>1 (selas_multi_slice.py:728-731) n'est donc couverte par aucun test.
- **Preuve** : tests/unit/test_multi_type_front.py:2298-2364 (seuls 2 DG → bloque, et 1 P + 1 DGA → OK ; aucun cas 2 Présidents ni plusieurs DGA). Recherche regex « deux président / Un seul Président / plusieurs DGA » dans tests/ : aucun résultat.
- **Correction** : Ajouter un test « 2 associés Président → blocage 'Un seul Président est admis' + bouton générer désactivé » et un test « Président + ≥2 DGA → pas de blocage ».

### [NITPICK] Incohérence de commentaire (hors verbatim O24-07 mais relevée) : le bandeau de bornes annonce « 2 a 5 associes » dans plusieurs commentaires/docstrings alors que la constante effective est SELAS_NB_MAX = 6, et le libellé UI affiche « 2 a 6 ». Le commentaire ment sur la borne réelle.
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:3 (« 2 a 5 associes ») et 86-88 (commentaire « 2 a 5 associes » juste au-dessus de SELAS_NB_MAX = 6) ; cf. libellé réel ligne 531 « {SELAS_NB_MIN} a {SELAS_NB_MAX} » = 2 a 6
- **Correction** : Mettre les commentaires/docstrings en cohérence avec SELAS_NB_MAX (2 à 6) ou corriger la constante si l'intention métier est réellement 5.

## O24-08 — DEFAUTS
### [MINEUR] Test faible : aucun test n'assert NEGATIVEMENT que le second champ (date de naissance / adresse) sous la case « Dirigeant » a bien disparu — c'est pourtant le coeur du verbatim O24-08 (« apparait deux fois -> une seule fois »). Le test UI test_front_selas_change_dirigeant_generates (tests/unit/test_multi_type_front.py:2233) prouve que la generation REUSSIT sans re-saisir date/adresse, mais ne verifie pas l'ABSENCE des cles selas_associe_X_sig_date_naissance / selas_associe_X_sig_adresse. Par contraste, test_selas_adresses_sur_une_ligne (l.2161-2163) fait bien des assertions negatives `not in keys` pour le siege. Une regression qui re-introduirait un second champ date/adresse sous le dirigeant passerait verte. Le risque de double-saisie n'est donc pas verrouille par un test dedie.
- **Preuve** : tests/unit/test_multi_type_front.py:2262-2267 (commentaire « plus de re-saisie d'adresse / date » mais aucune assertion `not in keys` correspondante) vs l.2161-2163 (assertions negatives presentes pour le siege)
- **Correction** : Ajouter dans un test UI SELAS pluripersonnelle des assertions negatives explicites : sous la case « Dirigeant » de l'associe coche, aucune cle de type selas_associe_X_sig_date_naissance / selas_associe_X_sig_adresse ne doit exister dans {w.key for w in app.text_input} (et aucun date_input dirigeant). Cela verrouille le verbatim O24-08 contre une regression de double-saisie.

### [NITPICK] Faute d'accent dans le label du champ date de naissance — UNIQUE saisie pivot du retour O24-08. Le label affiche « Date naissance » au lieu de « Date de naissance » (preposition manquante) ET sans accents dans l'exemple. Incoherence interne : le sous-formulaire cession (shell.py:2029) et la SELARL (shell.py:1280) ecrivent « Date de naissance ». Le formulaire au coeur du retour porte donc un libelle d'une qualite inferieure aux autres.
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:843 — `_ts(col_d, f"{prefix}_date_naissance", "Date naissance (ex: 1 janvier 1980)")`
- **Correction** : Harmoniser le libelle en « Date de naissance (ex : 1 janvier 1980) » (ajouter « de » + espace avant les deux-points, parite avec shell.py:1280/2029).

### [NITPICK] Accents manquants sur les libelles de filiation rendus sous la case « Dirigeant » (champs qui RESTENT a saisir apres O24-08) : « Nom du pere » et « Nom de la mere » au lieu de « Nom du père » et « Nom de la mère ». La convention projet (mémoire convention-mois-accentues + R12) impose les accents en sortie ; ces labels visibles a l'operateur derogent.
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:655-656 — `_ts(col_a, f"{prefix}_sig_nom_pere", "Nom du pere")` et `_ts(col_b, f"{prefix}_sig_nom_mere", "Nom de la mere")`
- **Correction** : Accentuer les labels : « Nom du père » et « Nom de la mère » (et idem la caption l.653 « Déclaration de non-condamnation »).

## O24-09 — DEFAUTS
### [MINEUR] Test faible : le seul test du retour O24-09 (test_selas_profession_field_removed) ne couvre que la MOITIÉ du verbatim. Il asserte uniquement l'absence d'un text_input dont la clé finit par « _profession » (suppression du champ profession), mais ne vérifie NI la présence du champ « qualification principale » (clé selas_associe_0_qualification), NI son libellé exact « Qualification principale (profession) » qui est le cœur de la demande (« mentionner (profession) dans le nom du champ »). Une régression sur ce libellé (parenthèse retirée, accent, faute) passerait inaperçue. De plus l'assertion `not any(k.endswith('_profession'))` ne porte que sur app.text_input : si un champ profession revenait sous forme de selectbox, le test ne le détecterait pas.
- **Preuve** : tests/unit/test_multi_type_front.py:2117-2118 — `keys = {str(w.key) for w in app.text_input}` puis `assert not any(k.endswith("_profession") for k in keys)` ; aucune assertion sur le libellé « Qualification principale (profession) » ni sur la présence du champ qualification
- **Correction** : Ajouter au test (ou en créer un dédié) deux assertions positives : (1) présence d'un text_input de clé « selas_associe_0_qualification », et (2) que son label est exactement « Qualification principale (profession) » (chaîne byte-exacte, parenthèses comprises) pour verrouiller le NOM du champ exigé par le verbatim.

### [NITPICK] Cohabitation potentiellement ambiguë de deux libellés « profession » dans le même écran SELAS pluripersonnelle : le menu société conserve le libellé « Profession » (selas_multi_slice.py:136) tandis que le champ associé est désormais « Qualification principale (profession) ». C'est légitime (le menu société = profession réglementée de la SEL, hors périmètre O24-09 qui vise l'associé) et donc conforme, mais aucune note/commentaire dans le code ne distingue explicitement que le « champ profession » supprimé par O24-09 est bien celui de l'associé et non le menu société — un relecteur futur pourrait croire à une suppression incomplète.
- **Preuve** : selas_multi_slice.py:136 (`profession = container.selectbox("Profession", ...)`) coexiste avec selas_multi_slice.py:870 (`"Qualification principale (profession)"`) ; le commentaire #9 ligne 847-851 ne précise pas que le menu société « Profession » est volontairement conservé
- **Correction** : Optionnel : ajouter une ligne de commentaire près du menu société (ligne 135-136) précisant que ce libellé « Profession » (profession réglementée de la société, R1) est distinct du champ associé supprimé par O24-09, pour lever toute ambiguïté de relecture.

## O24-10 — DEFAUTS
### [BLOQUANT] Retour O24-10 cassé pour le cas DENTAIRE — la dérivation produit l'INVERSE de l'intention. Le verbatim exige « 'dentaire' quand cas dentaire ». Or, après suppression du menu (commit d212e9a = O24-10), le type est forcé via _cession_default_type(profession) qui compare `profession == PROFESSION_DENTISTE`. PROFESSION_DENTISTE vaut "chirurgien_dentiste" (underscore, selarl_slice.py:92) MAIS le slice SELAS passe la chaîne du menu "chirurgien-dentiste" (tiret, selas_multi_slice.py:96-100 puis profession=profession ligne 375 -> _render_cession_form). Exécution réelle vérifiée : _cession_default_type("chirurgien-dentiste") = 'medical' (et non 'dentaire'). Comme le menu profession SELAS a pour DÉFAUT "chirurgien-dentiste" (1re option), le cas nominal d'un cabinet dentaire en SELAS génère les docs MÉDICAUX (DOC-009/010 au lieu de DOC-011/012, cf. selas_multi_slice.py:213-219) et affiche la caption « Type de cabinet : medical ». Avant O24-10 l'opérateur pouvait corriger via le selectbox ; le menu retiré, la valeur fausse est désormais forcée sans recours -> régression bloquante : impossible de produire une cession de cabinet dentaire en SELAS.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:1707-1708 (_cession_default_type) + :1939-1942 (forçage SELAS sans menu) ; selarl_slice.py:91-92 (PROFESSION_DENTISTE="chirurgien_dentiste") ; selas_multi_slice.py:96-100,293,375 (profession="chirurgien-dentiste" passée brute). Exécution : _cession_default_type("chirurgien-dentiste") -> 'medical'.
- **Correction** : Convertir la chaîne du menu SELAS vers la constante normalisée AVANT de la passer à _render_cession_form/_render_scm_cession_form (ex. mapper "chirurgien-dentiste"->PROFESSION_DENTISTE, "médecin"->PROFESSION_MEDECIN), OU rendre _cession_default_type/_profession_label tolérants (test `"dentiste" in profession.casefold()` comme déjà fait dans _scm_profession_pair shell.py:1728). Puis ajouter un test UI qui, profession dentaire sélectionnée + cession activée, vérifie type_cabinet=='dentaire' et la présence de DOC-011/012.

### [BLOQUANT] Même défaut de format contamine la NATURE DU FONDS LIBÉRAL dans les documents de cession. _profession_label(profession) (shell.py:1711-1712) compare aussi `profession == PROFESSION_DENTISTE` ; exécution vérifiée : _profession_label("chirurgien-dentiste") = 'médecin'. Cette valeur alimente la caption « Nature du fonds liberal : médecin » (shell.py:2159) ET les champs documentaires "nature_fonds_liberal" (shell.py:2226) et "profession" (shell.py:2598) -> les actes/compromis de cession d'un cabinet dentaire affichent « médecin ». Écart documentaire direct sur le livrable généré.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:1711-1712 (_profession_label), :1964 (profession_label=_profession_label(profession)), :2159 (caption), :2226 + :2598 (champs docs). Exécution : _profession_label("chirurgien-dentiste") -> 'médecin'.
- **Correction** : Corriger la source du paramètre profession (cf. B1) : une fois la valeur normalisée passée, _profession_label retournera 'chirurgien-dentiste'. Vérifier le rendu réel d'un acte de cession dentaire SELAS après correction.

### [MAJEUR] Test faible : aucun test ne couvre la dérivation profession->type_cabinet via l'UI, donc le bug B1/B2 passe inaperçu (11 tests cession verts malgré le bug). test_selas_cession_genere_acte_et_compromis_ensemble (test_multi_type_front.py:2065-2072) injecte type_cabinet="dentaire"/"medical" EN DUR dans le CessionContext au lieu de le dériver de la profession. test_selas_cession_vendeur_selectionnable (:2094) vérifie seulement l'ABSENCE du menu, jamais que la valeur dérivée est correcte pour un dentiste.
- **Preuve** : tests/unit/test_multi_type_front.py:2065-2072 (type_cabinet en dur) et :2091-2094 (assert d'absence du menu uniquement). pytest -k cession : 11 passed malgré _cession_default_type("chirurgien-dentiste")=='medical'.
- **Correction** : Ajouter un test d'intégration UI : sélectionner profession dentaire dans le formulaire SELAS, activer la cession, et asserter que la caption affiche « dentaire », que document_codes contient DOC-011/DOC-012 et non DOC-009/DOC-010, et que la nature du fonds = chirurgien-dentiste.

### [MINEUR] Incohérence interne de traitement de `profession` dans le même sous-formulaire de cession SELAS : la branche SCM (_scm_profession_pair, shell.py:1728) gère correctement la chaîne du menu via `"dentiste" in profession_label.casefold()`, tandis que la branche cabinet (_cession_default_type / _profession_label) échoue sur la même valeur. Deux chemins du même écran interprètent la profession différemment -> SCM dentaire OK, cession cabinet dentaire KO.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:1728 (SCM tolérant) vs :1708 et :1712 (cabinet strict ==). selas_multi_slice.py:1396-1412 passe la même `profession` aux deux.
- **Correction** : Uniformiser la normalisation de la profession en un seul point avant dispatch vers les deux sous-formulaires, pour éliminer la divergence de comportement.

### [NITPICK] Écart cosmétique au verbatim : O24-10 dit « SUPPRIMER ce champ, cela va de soi selon le cas » — la suppression du widget de saisie est faite, mais une caption « Type de cabinet : ... (derive de la profession). » réintroduit un libellé affiché du même intitulé. De plus « derive »/« derivee » ne sont pas accentués (caption :1942 et :2159) alors que la convention de sortie privilégie les accents. Secondaire devant les bugs ci-dessus mais à signaler.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:1942 (« derive de la profession ») et :2159 (« derivee de la profession »).
- **Correction** : Statuer sur le maintien d'une caption informative (le verbatim demande la suppression pure) ; si conservée, accentuer « dérivé/dérivée ».

## O24-11 — DEFAUTS
### [MAJEUR] Reprise infidele du regime matrimonial du vendeur selectionne. Le verbatim exige que « toutes ses informations soient retranscrites directement ». Or le regime matrimonial precis est perdu : `_situation_display(situation_label, genre)` ecrase les 4 presets maries (communaute legale, separation de biens, communaute universelle, participation aux acquets) en un simple « marie »/« mariee » (table fermee, selas_multi_slice.py:908-922). Cette valeur ecrase ensuite la cle de session lue par la cession (`st.session_state['selas_situation_maritale'] = praticien['situation_maritale']`, selas_multi_slice.py:1392). En aval, `_vendeur_regime_label` (shell.py:1819-1834) ne voit que « marie » -> startswith('mari') vrai, aucun mot-cle universelle/separation/participation -> retourne TOUJOURS le defaut « communaute reduite aux acquets ». Resultat : un vendeur marie sous separation de biens / communaute universelle / participation aux acquets est rendu dans l'acte de cession avec le MAUVAIS regime. Regression specifique a la SELAS : le chemin SELARL de reference transmet le LIBELLE COMPLET du menu via la cle du selectbox (shell.py:1389-1392), donc `_vendeur_regime_label` y recoit le bon libelle.
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:908-922 (collapse) + :1392 (ecrasement cle) ; src/sydel_doc_engine/front_app/shell.py:1819-1834 (_vendeur_regime_label) vs :1389-1392 (chemin SELARL correct)
- **Correction** : Propager le LIBELLE DE MENU complet de la situation matrimoniale de l'associe choisi (preset MATRIMONIAL_STATUS_PRESETS) dans `selas_situation_maritale` / `praticien['situation_maritale']`, et non le mot resolu de `_situation_display`. Le `_render_selas_cession` doit lire la cle `selas_associe_{vendeur_index}_situation` (le libelle brut du selectbox de l'associe) et la reinjecter telle quelle, comme le fait la SELARL, pour que `_vendeur_regime_label` derive le bon regime.

### [MAJEUR] Le conjoint du vendeur selectionne n'est jamais retranscrit, alors que le verbatim impose « toutes ses informations ». Le dict `praticien` construit pour la cession SELAS (selas_multi_slice.py:1375-1390) ne contient AUCUNE cle conjoint (`conjoint_civilite`, `conjoint_prenom`, `conjoint_nom`). Les infos du conjoint de l'associe existent pourtant (saisies dans `regime_communautaire_associe` via `_render_conjoint_si_communaute`, selas_multi_slice.py:925-951) mais ne sont jamais reportees dans le dict praticien. En consequence, dans le chemin vendeur_auto, `conjoint_payload` (shell.py:1971-1975) lit `praticien.get('conjoint_*')` -> chaines vides, donc l'acte de cession d'un vendeur marie sort sans nom de conjoint.
- **Preuve** : src/sydel_doc_engine/front_app/selas_multi_slice.py:1375-1390 (praticien sans conjoint) ; src/sydel_doc_engine/front_app/shell.py:1971-1975 (conjoint_payload lit praticien.get('conjoint_*'))
- **Correction** : Ajouter au dict `praticien` de `_render_selas_cession` les cles `conjoint_civilite`/`conjoint_prenom`/`conjoint_nom` derivees de `vendeur.regime_communautaire_associe` (ou de la cle de session `selas_associe_{i}_conjoint_*`) afin que l'etat civil du conjoint du vendeur choisi soit reellement retranscrit dans l'acte.

### [MINEUR] Wording « associe unique » residuel dans le MEME flux de cession vise par le verbatim. La legende du sous-bloc « Avenant de bail » affiche « Le locataire actuel est l'associe unique ; ... » sans aucune garde sur le prefixe : elle s'affiche donc en SELAS multi (le bail est rendu dans `_render_cession_form` appele avec prefix='selas', shell.py:1396/2534-2538). Contrairement au libelle vendeur (shell.py:1981-1984) qui bascule bien sur 'l'associe selectionne ci-dessus', cette legende n'a pas ete corrigee -> contradiction interne (« associe unique » faux en multi-associes). De plus faute d'accent : « associe » au lieu d'« associe » accentue.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:2536
- **Correction** : Conditionner la legende du bail sur le prefixe (« Le locataire actuel est l'associe selectionne / le cedant » en SELAS) comme le fait deja le libelle vendeur, et accentuer « associe » -> « associé ».

### [MINEUR] Test faible : le seul test de cette feature ne couvre PAS le coeur du verbatim. `test_selas_cession_vendeur_selectionnable` se contente d'asserter (a) la presence de la cle de selectbox `selas_cession_vendeur_index` et (b) l'absence du wording « associe unique » dans les checkboxes. Il ne SELECTIONNE jamais un associe autre que le defaut, et n'assert JAMAIS que les informations de l'associe choisi (nom, adresse, situation, regime, conjoint) sont effectivement retranscrites dans le contexte de cession. La seconde moitie du verbatim (« CHOISIR l'un des associes ... que toutes ses informations soient retranscrites directement ») est donc non testee, ce qui masque les deux defauts MAJEURS ci-dessus.
- **Preuve** : tests/unit/test_multi_type_front.py:2075-2099
- **Correction** : Ajouter un cas qui (1) cree >=2 associes physiques distincts, (2) selectionne via `selas_cession_vendeur_index` un associe NON-president, (3) construit le contexte de cession et assert que prenom/nom/adresse/date de naissance/situation matrimoniale ET regime matrimonial ET conjoint du contexte cession correspondent exactement a l'associe choisi (et non au president).

### [NITPICK] Wording « associe unique » residuel egalement dans le sous-formulaire de cession de parts SCM, lui aussi reutilise en SELAS multi (prefix='selas', selas_multi_slice.py:1405-1412). La legende du bloc « Cedant » affiche « Preremplie avec l'associe unique ; modifiable si besoin. » Hors perimetre strict du retour O24-11 (qui vise la cession de cabinet) mais meme famille de wording obsolete, faux en multi-associes, et accent manquant.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:2682
- **Correction** : Remplacer par un libelle neutre en SELAS (« Prerempli avec le cedant selectionne ; modifiable. ») et accentuer « associe » -> « associé ».

## O24-12 — DEFAUTS
### [MAJEUR] Test non-discriminant (règle 65 §3 : un test bien conçu doit prouver ce qu'il affirme). Le test `test_selas_cession_cabinet_meme_adresse_lieu_exercice` asserte `cab.value == "5 place du Centre, 69000 Lyon"`, MAIS dans les données de test seedées, le siège (`selas_siege`/`selas_siege_adresse`) ET le lieu d'exercice (`selas_adresse_lieu_exercice`) valent la MÊME chaîne « 5 place du Centre, 69000 Lyon ». Or l'adresse du cabinet est de toute façon pré-remplie avec le siège via `_cession_text(default=siege_display)`. Le test passe donc même si le report depuis le lieu d'exercice ne fonctionnait pas — il ne distingue pas « report O24-12 » de « pré-remplissage siège ». J'ai vérifié moi-même : en rendant le lieu d'exercice distinct (« 99 avenue Distincte, 75001 Paris »), le report FONCTIONNE réellement (cabinet = 99 avenue Distincte), donc le code est bon — mais le test ne le prouve pas et resterait vert en cas de régression du report.
- **Preuve** : tests/unit/test_multi_type_front.py:2180 (assert cab.value == "5 place du Centre, 69000 Lyon") ; shell.py:796-797 (selas_siege/selas_siege_adresse = « 5 place du Centre, 69000 Lyon ») ; shell.py:805 (selas_adresse_lieu_exercice = « 5 place du Centre, 69000 Lyon » — valeur IDENTIQUE)
- **Correction** : Rendre le test discriminant : seeder un lieu d'exercice DIFFÉRENT du siège, cocher la case, puis asserter que l'adresse du cabinet vaut le LIEU D'EXERCICE et NON le siège. Ajouter aussi un cas case décochée pour vérifier que le cabinet ne reprend PAS le lieu d'exercice.

### [MINEUR] Écart au verbatim sur la condition d'affichage de la case. Le verbatim dit « adresse du cabinet -> ajouter une case » (inconditionnel). Le code masque la case quand le lieu d'exercice est vide (`if prefix == "selas" and lieu_exercice:`). Si l'opérateur remplit le sous-formulaire cession AVANT le champ « Adresse lieu d'exercice » (situé tout en haut du formulaire, la cession étant tout en bas), la case n'apparaît pas du tout. La case devrait exister dès que le bloc Cabinet est affiché en SELAS, et se contenter de ne rien reporter si le lieu d'exercice est vide.
- **Preuve** : shell.py:2166 (`if prefix == "selas" and lieu_exercice:` — la case n'est rendue que si lieu_exercice est non vide)
- **Correction** : Afficher la case dès que `prefix == "selas"` (indépendamment de la valeur), et ne reporter que si le lieu d'exercice est renseigné (sinon ne rien copier). Conforme au verbatim « ajouter une case » sans condition de présence préalable de donnée.

### [MINEUR] Régression latente du pré-remplissage siège en SELAS via le passage de `if` à `elif`. Avant le commit, le pré-remplissage siège (`st.session_state[cab_key] = siege_display` si vide) s'appliquait à tous les préfixes. Après, en SELAS avec lieu d'exercice présent, la branche `elif` est court-circuitée par le `if` même quand la case n'est PAS cochée — le pré-remplissage siège ne passe alors plus par cette branche. Le filet de secours reste le `default=siege_display` de `_cession_text` (vérifié : le cabinet reste pré-rempli au siège quand la case est décochée), donc pas de casse observable en pratique — mais la logique est désormais fragile et dépend d'un effet de bord du `default` plutôt que d'une intention explicite.
- **Preuve** : shell.py:2166-2174 (le `if prefix=="selas" and lieu_exercice:` capture le flux et le `elif` siège ne s'exécute jamais en SELAS dès qu'un lieu d'exercice existe, case cochée ou non)
- **Correction** : Découpler les deux logiques : (1) pré-remplir le cabinet avec le siège (comportement de base, tous préfixes), puis (2) si la case est cochée, écraser avec le lieu d'exercice. Éviter le `elif` qui rend le pré-remplissage siège SELAS dépendant uniquement du `default` de `_cession_text`.

### [NITPICK] Le libellé de la case ajoute un préfixe « Adresse du cabinet = » au texte exact du verbatim. Le verbatim ne nomme la case que « même adresse que le lieu d'exercice ». Le préfixe est défendable (il situe la case sur l'adresse du cabinet) et le segment verbatim « même adresse que le lieu d'exercice » est présent mot pour mot avec les bons accents, mais ce n'est pas la formulation exacte demandée.
- **Preuve** : shell.py:2168 (« Adresse du cabinet = même adresse que le lieu d'exercice ») vs verbatim « ajouter une case : "même adresse que le lieu d'exercice" »
- **Correction** : Si la fidélité stricte au verbatim est exigée, libeller la case exactement « même adresse que le lieu d'exercice » (le contexte « Cabinet » étant déjà donné par l'expander). À confirmer Rafael — choix cosmétique.

## O24-13 — RIEN_A_REDIRE
RIEN À REDIRE.

## O24-14 — DEFAUTS
### [BLOQUANT] Crash runtime a la generation : en SELAS, le formulaire FORCE cession.etape='acte' (shell.py:1955-1956), mais l'orchestrateur selectionne ENSEMBLE l'acte ET le compromis pour le type de cabinet (service.py:365-366 -> _cession_cabinet_enabled renvoie True pour DOC-011 ET DOC-012 / DOC-009 ET DOC-010). Le generateur du compromis (cession_cabinets_common.py:1069-1070, _validate_selection) leve une ValueError des que cession.etape != 'compromis'. Resultat : des qu'un utilisateur SELAS active une cession, generate_dossier crashe sur le compromis. REPRODUIT end-to-end (clone claude, sys.path force) : dentaire -> 'ValueError: cession.etape doit etre compromis pour compromis_cession_cabinet_dentaire.docx' ; medical -> idem pour compromis_cession_cabinet_medical.docx. Le verbatim O24-14 (remplir A LA FOIS l'acte ET le compromis) n'est donc PAS livre : au lieu des deux documents, l'utilisateur obtient une exception. Le single CessionContext (etape='acte') est passe inchange a chaque generateur (build_generation_context selas_multi_slice.py:1494 ; generate_documents service.py:277) — aucun remap d'etape par document.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:1954-1956 (etape='acte' force en SELAS) + src/sydel_doc_engine/orchestrator/service.py:365-366 (SELAS -> les deux docs actives) + src/sydel_doc_engine/generators/lot_03/cession_cabinets_common.py:1069-1070 (raise si etape != variant.etape). Repro : ui_runtime.generate_docx_files_for_document_codes(ctx_SELAS_dentaire_etape_acte, out, ('DOC-011','DOC-012')) -> 'CRASH: ValueError -> cession.etape doit etre compromis pour compromis_cession_cabinet_dentaire.docx'.
- **Correction** : Rendre l'etape NON pertinente pour la generation en SELAS : soit cloner le CessionContext par document avec l'etape attendue avant d'appeler chaque generateur (etape='acte' pour DOC-009/011, etape='compromis' pour DOC-010/012), soit relacher le garde _validate_selection cote SELAS, soit deriver l'etape du variant et non du contexte. Couvrir par un test e2e qui GENERE reellement les deux documents en SELAS (pas seulement les codes du plan).

### [MAJEUR] Champs SPECIFIQUES au compromis inatteignables en SELAS : meme une fois le crash corrige, le compromis ne peut pas etre 'rempli' comme l'exige le verbatim. Le pret (Montant du pret, Taux, Duree) n'est saisissable que 'if etape == compromis' (shell.py:2397) et la 'Date limite de realisation' que 'if etape == compromis' (shell.py:2527). Comme l'etape est forcee a 'acte' en SELAS, ces champs ne sont JAMAIS affiches : pret_payload reste {montant:'',taux:'',duree:''} (shell.py:2396) et date_limite_realisation reste '' (shell.py:2526). Verifie cote moteur : ces valeurs vides sont injectees via put_opt ([montant_pret]/[taux_pret]/[duree_pret] l.768-770, [date_realisation_limite] l.777 de cession_cabinets_common.py) -> le compromis genere sortirait avec une clause de pret VIDE et une date limite VIDE. Le verbatim dit 'remplir a la fois l'acte AINSI QUE le compromis' : l'operateur n'a aucun moyen de remplir les donnees propres au compromis.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:2396-2397 (pret gate compromis) ; src/sydel_doc_engine/front_app/shell.py:2526-2531 (date_limite gate compromis) ; src/sydel_doc_engine/generators/lot_03/cession_cabinets_common.py:768-770 et 777 (tokens compromis injectes vides via put_opt).
- **Correction** : En SELAS, exposer les champs propres au compromis (pret montant/taux/duree, date limite de realisation) en plus des champs propres a l'acte, puisque les DEUX documents sont desormais produits. Retirer le gating 'if etape == compromis' pour le chemin SELAS (ou collecter ces champs inconditionnellement quand une cession SELAS est active).

### [MAJEUR] Test faible : le seul test dedie a O24-14 (test_selas_cession_genere_acte_et_compromis_ensemble) verifie UNIQUEMENT que le helper de plan _cession_codes renvoie les deux codes (DOC-011/DOC-012, DOC-009/DOC-010) — il ne genere AUCUN document. Aucun test SELAS e2e (generate_dossier) n'active de cession_context : les tests cession SELAS (test_multi_type_front.py:1956,1988,2059) ne touchent que les codes/plan ou la presence de widgets. Le crash bloquant decrit ci-dessus passe donc 'vert' alors que la fonctionnalite est cassee au runtime. Mock/assertion qui deguise une vraie absence (regle 65).
- **Preuve** : tests/unit/test_multi_type_front.py:2059-2072 (assert sur _cession_codes seulement, pas de generation) ; aucun appel selas_multi_slice.generate_dossier avec cession_context dans le fichier (grep cession+generate_dossier = uniquement spfpl).
- **Correction** : Ajouter un test e2e qui appelle selas_multi_slice.generate_dossier (ou ui_runtime.generate_docx_files_for_document_codes) avec un cession_context SELAS complet et verifie que le ZIP contient REELLEMENT acte ET compromis, avec leurs clauses (pret, date limite) renseignees — pas seulement les codes du plan.

### [MINEUR] Incoherence/odeur de contrat : le formulaire force etape='acte' puis ecrit cette valeur dans le payload ('etape': etape, shell.py:2557) et dans CessionContext (l.2578), alors que ce champ ne doit plus avoir de sens en SELAS (l'etape n'est 'plus filtrante', cf. commentaire service.py:363-364). Conserver un etape='acte' menteur dans le contexte est precisement ce qui declenche le crash du compromis. Le champ etape reste un piege : tout futur generateur qui s'appuie sur ctx.cession.etape en SELAS recevra 'acte' a tort.
- **Preuve** : src/sydel_doc_engine/front_app/shell.py:2557 et 2578 (etape='acte' fige ecrit dans le contexte) vs commentaire src/sydel_doc_engine/orchestrator/service.py:362-364 ('l'etape n'est plus filtrante').
- **Correction** : Neutraliser explicitement l'etape pour le chemin SELAS (p.ex. la deriver par document au moment de la generation) plutot que de figer 'acte' dans un contexte partage entre deux documents d'etapes differentes.

## O24-15 — RIEN_A_REDIRE
RIEN À REDIRE.
