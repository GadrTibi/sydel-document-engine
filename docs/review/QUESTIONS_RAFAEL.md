# Questions en attente pour Rafael / Albane — refonte SELAS (onglet 24)

> Points où un arbitrage **métier / wording d'acte** est nécessaire. Politique (Gad 2026-06-22) :
> on NE bloque PAS et on NE demande PAS à Gad — on applique le **défaut le plus simple / sûr** ci-dessous,
> on accumule ici, Rafael/Albane tranchent plus tard, on rajuste au besoin.

| # | Question | Défaut appliqué (réversible) |
|---|---|---|
| #9 | En retirant le champ « profession » (garder « qualification »), la comparution doit-elle **garder le titre « Docteur »** (« Docteur Médecin généraliste… ») ou **ne montrer que la qualification** (« Médecin généraliste… ») ? | **Titre « Docteur » conservé et dérivé** → output de l'acte INCHANGÉ ; seul le champ disparaît du formulaire. |
| #2 ✅ TRANCHÉ (Rafael 2026-06-23) | DNC par dirigeant ou une seule ? | **RÉPONDU : une DNC PAR dirigeant** (Président + chaque DG / DG Associé). Implémenté (`4622b8a`). |
| Appel de fonds (DOC-008) | En cession de cabinet **SELAS**, faut-il générer un **appel de fonds** comme en SELARL ? | **Non inscrit** au plan SELAS (le moteur le restreint à la SELARL). |
| #12 | La case « siège = lieu d'exercice » remplace l'ancienne « siège = adresse président » : on garde uniquement « = lieu d'exercice », ou les deux ? Et le **lieu d'exercice** reste 1 champ libre (parsé vers le siège structuré) — faut-il le **restructurer en 4 champs** (No/Voie/CP/Ville) ? | « = lieu d'exercice » **remplace** « = président » ; lieu d'exercice **reste 1 champ** parsé best-effort. |
| B3 (technique) | Le champ libre « Siège (adresse affichée) » est redondant avec les 4 champs structurés (SELAS uni est propre). Le retirer changerait la validation « Siège requis ». | **Conservé** pour l'instant (retrait = dette technique, sans impact métier). |
| O24-07a | En SELAS, le **Président est-il OBLIGATOIRE** (cardinalité min 1) ? Le verbatim dit « soit président (un seul) » sans trancher explicitement le caractère obligatoire. | **Oui, Président obligatoire** (bloqué si dirigeants désignés sans Président). Source NotebookLM : SELAS « centrée sur la présidence ». |
| O24-07b | Le verbatim « fonctions non cumulatives » autorise-t-il la **coexistence Président + Directeur Général + plusieurs DGA** dans la même société, ou exige-t-il un mode de gouvernance exclusif ? | **Coexistence autorisée** : 1 Président + 1 DG + plusieurs DGA (non-cumul PAR personne via le menu mono-rôle). |
| O24-03 | Une adresse **sans numéro de voie** (lieu-dit, place) est-elle acceptable pour ces actes, ou le **numéro est-il obligatoire** ? | **Numéro REQUIS** (comportement historique maintenu, cohérent avec `_validate_common_docs` qui exige `siege_num`/`signataire_adresse_num`). Lieu-dit **non supporté** tant que non tranché — pas d'extrapolation. |
| O24-11 PACS | Le verbatim O24-11 vise le vendeur **marié**. Un vendeur **pacsé** : son partenaire doit-il figurer à l'acte de cession (il n'existe pas de segment « pacsé avec [conjoint] » dans les modèles) ? | **PACS exclu** de la capture conjoint (sinon donnée saisie jamais retranscrite). Capture conjoint = mariage uniquement. À confirmer Albane si le PACS doit être traité. |
| O24-12 | Le libellé de la case est « **Adresse du cabinet = même adresse que le lieu d'exercice** » (préfixe « Adresse du cabinet = » ajouté au verbatim « même adresse que le lieu d'exercice »). Préfixe acceptable ou libellé strict ? | **Préfixe conservé** (plus clair en contexte) ; verbatim contenu intégralement. |
| O24-fidélité-SELARL | Le mot littéral « **SELARL** » figé dans les **modèles source de cession** apparaît à DEUX endroits distincts : (1) la **ligne forme acquereur** « SELARL au capital de [capital] » — modèle de l'**acte médical** uniquement (les 3 autres modèles utilisaient déjà le token `[forme_sociale_acquereur]`) ; (2) le **titre de section** « Inscription de la SELARL au Tableau de l'Ordre… » dans les **compromis médical & dentaire**. Pour une cession SELAS ces wordings affichaient « SELARL ». | **(1) CORRIGÉ (2026-06-24, `d18c0fd`)** — segment override « SELARL au capital de » → « [forme_sociale_acquereur] au capital de » : l'acte médical SELAS affiche désormais « SELAS au capital de » ; sur un acquereur SELARL le token se remplit « SELARL » → byte-identique (gold intact). **(2) DETTE OUVERTE** — « Inscription de la SELARL au Tableau » reste figé dans les 2 compromis : c'est un **titre de clause** = arbitrage métier Albane avant toute édition (un line_fix conditionné SELAS est trivial techniquement, mais on n'édite pas un wording d'acte sans GO Albane). |
| TRACE-a — bail-seul plan menteur SELAS | `build_selas_plan` annonce **DOC-007** (avenant de bail) dès qu'un `bail_context` est présent **même sans** `cession_context`. Or `_cession_codes` n'émet l'avenant que si une cession cabinet existe → un dossier *bail seul* (sans cession) annoncerait DOC-007 sans le générer (plan menteur théorique). | **INATTEIGNABLE via l'UI** : la case cession ET le bloc bail sont pilotés par le **même checkbox** `selas_cession_on` (impossible d'avoir un `bail_context` sans `cession_context`). **Dette tracée, non corrigée.** Garde simple possible si jamais découplé : n'inscrire DOC-007 que si `cession_context` est aussi présent. |
| TRACE-b — `_rename_with_slug` repli | `_rename_with_slug` (renommage des lettres de régime par associé marié multiple) retourne `base_target` **sous collision ET source absente** (cas de repli silencieux). | **INATTEIGNABLE** : répertoire de sortie temp **frais** par génération + **jamais deux associés au même slug** (slug = nom de l'associé). Aucun scénario réel n'atteint ce repli. **Limite assumée, non corrigée.** |
| TRACE-c — fragilité xdist (chemins relatifs `artifacts/`) | Certains chemins de test sont relatifs (`artifacts/…`) → la suite **n'est déterministe qu'en séquentiel** ; en parallèle (`pytest -n`/xdist) des collisions de fichiers sont possibles. | **Mode d'exécution = séquentiel** (conftest autouse isole `ARTIFACTS_DIR` par test). **Ordre-indépendance PROUVÉE (T9-T10)** : suite verte sur plusieurs ordres de collecte distincts (`pytest-randomly`). Décompte courant **594** (les seeds 1/2/3 tournaient à 593 avant l'ajout du test SELARL au T9 ; reconfirmé 594 sur l'ordre par défaut + seeds 7/99 au gate de convergence T10). `pytest-randomly` (>=3.15,<5) en dep dev, ordre randomisé par défaut. **xdist non utilisé** (parallélisme processus = autre sujet) ; dette tracée. |
| TRACE-e — retry FS dans `zip_bundle._validate_source_path` | Le petit retry borné (≤ 0,2 s) ajouté pour fiabiliser le bundle sous course FS génération→ZIP est une **préoccupation de robustesse runtime** que certains liraient comme « logique de test dans le code de prod ». | **Conservé** : la course FS (document.save pas encore visible par `is_file()` sous pression disque) est un risque RÉEL en prod Streamlit Cloud, pas un artefact de test. Retry borné + erreur explicite si dépassé. Nitpick assumé, non corrigé. |
| TRACE-f — `date.today()` en valeurs par défaut de widgets | `date.today()` est lu à de nombreux endroits (`shell.py`, `front_widgets.py`, slices) → dépendance temporelle théorique. | **AUCUN impact détermisme DOCX** : usage = **prefill de widgets UI** (la date réelle est choisie par l'utilisateur / figée par les fixtures de test, ex. `date(2026,5,14)`). Aucun `date.today()` dans `generators/` (vérifié T10). **Cas-limite tracé [n1]** : `data_entry.py:139` est un prefill de *valeur de données* (`date_courrier_avertissement`), pas un widget `value=` — mais il est inerte côté DOCX : le générateur `lettre_renonciation_associe` lit la date du **contexte** (`signature_date` câblée par les slices) et **lève `ValueError`** si absente, il ne retombe jamais silencieusement sur la date du jour. `field_derivations.today()` n'a aucun appelant. Nitpick assumé. |
| TRACE-d — mutation in-place de Cession/ScmCessionContext | `build_generation_context` **mute en place** `cession_context.acquereur.forme_sociale` et `scm_cession_context.variante_structure` (post-corrections SELAS-only). | **PAS D'IMPACT PROD** : le contexte est **reconstruit à chaque rerun** Streamlit (les `*_context` du payload sont re-dérivés des sous-formulaires) → la mutation ne persiste pas d'un run à l'autre. Limite assumée ; une copie défensive (`model_copy`) supprimerait la mutation si un appel batch réutilisait le même objet payload. |
| O24-05 valeur nominale cible SPFPL | La **valeur nominale de la société CIBLE** d'un apport/cession SPFPL (société externe transcrite, pas un type généré par le moteur) reste en **saisie libre** — aucune valeur fractionnaire n'atteint un DOCX (backstop `number_words_from_value`). Est-elle dans le périmètre « valeur nominale calculée automatiquement » (O24-05) ou **exclusion assumée** (société tierce non maîtrisée) ? | **Exclusion assumée** (saisie libre conservée ; la cible est une société externe, pas un capital piloté par le moteur). À confirmer Albane. |
| Salarié cession incomplet (acte dentaire) | L'acte de cession dentaire **lève** si un salarié est ajouté sans identité complète (nom/prénom requis). Le **formulaire doit-il bloquer** la soumission d'un salarié incomplet, ou l'échec moteur est-il **volontaire** (garde dure) ? | **Échec moteur volontaire** (garde dure côté générateur ; pas de garde formulaire ajoutée — comportement actuel conservé). À confirmer Rafael. |
| SELAS cession SCM standalone ✅ CÂBLÉ (2026-06-23, feat/propagation-q4-adresses) | Le **plan** SELAS inscrit bien DOC-031/032/033 quand la **cession de parts SCM seule** est cochée, mais `selas_multi_slice.generate_dossier` **n'émettait PAS** ces fichiers. Par ailleurs la **cession de cabinet SELAS** levait `document est obligatoire pour CODE-CESSION-CAB-001` (l'acte/compromis ET l'avenant de bail exigent `ctx.document`, non fourni par le slice SELAS — fourni seulement par SELARL). | **CÂBLAGE TECHNIQUE FAIT** (aucun contenu juridique changé). `build_generation_context` SELAS fournit désormais `document=DocumentContext(...)` (comme la SELARL) ET normalise `scm_cession.variante_structure` → `'selas'` (sinon plan menteur DOC-031/032/033). `generate_dossier` SELAS émet maintenant acte+compromis cabinet, avenant bail, DOC-031/032/033 SCM **sans crash** (prouvé e2e : `test_selas_cession_generate_dossier_emits_all_cession_files`). Reste l'arbitrage Albane `O24-fidélité-SELARL` ci-dessus (wording figé « SELARL »), indépendant du câblage. |
| TACHE-C-1 — `is_capital_divisible` faux-positif gros capitaux | `is_capital_divisible` peut renvoyer un faux-positif pour des capitaux à **28+ chiffres** (précision flottante au-delà de la plage métier réelle). | **Limite ASSUMÉE** — hors plage métier réelle (un capital de SEL/SCI n'atteint jamais 28 chiffres). Aucun impact pratique ; non corrigé. À ne signaler que si un capital de cette ampleur devenait plausible. |
| TACHE-C-2 — `accentuate_french_months` Titlecase | `accentuate_french_months` rend « DECEMBRE » → « **Décembre** » (Titlecase) et non « DÉCEMBRE » (UPPERCASE conservé). | **Limite CONNUE, impact NUL aujourd'hui** — aucun modèle source n'imprime un mois en capitales dans un champ re-accentué en amont. Non corrigé (réversible si un cas UPPERCASE apparaît). |
| TACHE-C-3 — contenu métier révélé par l'émission cession SELAS | Le câblage de la cession SELAS (TACHE A) émet les actes via les **mêmes générateurs que la SELARL**. Sur le scénario testé (fixtures cession SELARL valides) **aucun champ obligatoire vide ni clause inconnue n'a bloqué** l'émission. | **Aucun besoin de contenu métier neuf révélé** par l'émission. Seul arbitrage ouvert = `O24-fidélité-SELARL` (wording figé « SELARL » des modèles de cession) — déjà tracé, inchangé. |
| L6 — docs renonciation/avertissement pour une SPFPL (holding) | LIVE-06 « ne sert à rien » a été traité comme « la **case à cocher** est redondante » → remplacée par un menu de régime qui **conserve** la génération DOC-005 (renonciation) + DOC-006 (avertissement au conjoint) en communauté légale. Mais « ne sert à rien » pourrait aussi vouloir dire que **ces documents eux-mêmes n'ont pas lieu d'être pour une holding SPFPL** (vs une société d'exercice). | **Comportement PRÉSERVÉ par défaut** (les docs restent générés en communauté légale, comme avant — fix non destructif). **À confirmer Rafael/Albane** : si une SPFPL holding ne doit PAS produire ces 2 lettres, on retire le mécanisme entièrement (régime devient cosmétique). Non supposé sans confirmation. |
